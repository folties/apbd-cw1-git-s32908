1. Fast forward is used when the main branch has not new commits after the feature branch was created, and the merge commit is created when both branches have no commits and their histories diverge. 

2. Merge is used when you want to combine branches and save the real branch history. It often creates a merge commit. Rebase moves commits from one branch on top of another branch and makes the history more linear 

3. The conflitct was resolved by manually merging two console log outputs in one, and after resolving conflict the file was added and commited.
